name: C/C++ CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - run:  sudo apt-get install gcc-multilib g++-multilib libxext-dev && sudo apt install libx11-dev libpng-dev
    - run: cd WaspSrc && make clean && make
