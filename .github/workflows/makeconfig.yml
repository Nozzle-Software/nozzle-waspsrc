name: C/C++ CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - run:  sudo apt-get install gcc-multilib g++-multilib && sudo apt install x11-utils x11-apps
    - run: cd WaspSrc && make clean && make
